# Copiar a .env y completar. NUNCA commitear .env con valores reales.
# Las claves de IA y los modelos NO van aqui: viven en la base de datos
# (tablas api_keys y session_settings - ver src/db/seed_llm.py y los
# endpoints /api-keys y /settings).

POSTGRES_USER=ergalia_user
POSTGRES_PASSWORD=cambia_esto_por_un_valor_generado
POSTGRES_DB=escape_ergalia_os

DATABASE_URL=postgresql+psycopg2://ergalia_user:cambia_esto_por_un_valor_generado@db:5432/escape_ergalia_os
DATABASE_URL_ASYNC=postgresql://ergalia_user:cambia_esto_por_un_valor_generado@db:5432/escape_ergalia_os

NOVELTY_SCORE_THRESHOLD=4

# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
