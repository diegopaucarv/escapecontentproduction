# Versiones verificadas contra PyPI el 2026-09-12.
# La propuesta original fijaba langchain==0.1.16 / langgraph==0.0.38 —
# son versiones pre-1.0 de 2024; la API cambió de forma incompatible
# desde entonces (StateGraph, checkpointers, interrupt() para
# human-in-the-loop). No se puede migrar cambiando solo el número de
# versión: cualquier código de agentes que se escriba debe hacerse
# contra la API 1.x, no contra estos ejemplos de hace dos años.

# Web / API
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Datos
sqlalchemy==2.0.52
psycopg2-binary==2.9.13   # driver síncrono (API, migraciones)
asyncpg==0.31.0           # driver async (listener LISTEN/NOTIFY)
pgvector==0.5.0
alembic==1.20.0

# Configuración
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Resiliencia
tenacity==9.1.4
httpx==0.28.1

# Autenticación (RBAC — Líder / Equipo / Externo, ver src/auth.py)
pyjwt==2.14.0
bcrypt==5.0.0
python-multipart==0.0.32   # requerido por OAuth2PasswordRequestForm de FastAPI

# Orquestación agencial — solo si se usa el bucle Producer-Critic de
# Full Development (ver README y src/agents/producer_critic.py). No es
# una dependencia dura del resto del sistema: la API y el listener
# funcionan sin ella.
langgraph==1.2.11
langchain-anthropic==1.7.2
# langgraph-checkpoint-postgres  # activar cuando el checkpointer pase
# de InMemorySaver (esqueleto) a persistencia real entre reinicios.

# Embeddings locales (jina-embeddings-v5-text-nano vía transformers.AutoModel).
# Inferencia LOCAL: el modelo se descarga desde HuggingFace Hub y se ejecuta
# en el contenedor — no hay API externa.
# OJO: el modelo Jina usa trust_remote_code y su código remoto importa módulos
# SOLO de transformers 5.x (modeling_flash_attention_utils, ALL_ATTENTION_FUNCTIONS,
# processing_utils.Unpack). Con transformers 4.x falla con el confuso error
# "Could not import module 'XLMRobertaModel'". Mantener transformers==5.1.0.
# torch NO va aquí: el wheel depende del hardware (CPU vs CUDA). Se instala
# en el Dockerfile (ARGs TORCH_INDEX_URL/TORCH_PACKAGE, default CPU) o con
# scripts/install_torch.py (auto-detección) en instalaciones fuera de Docker.
transformers==5.1.0
peft==0.20.0   # jina-embeddings-v5 usa adapters LoRA (PEFT)

# Sistema KAG — segmentador (src/kag/segmentador.py) y consulta (src/kag_query.py).
# El segmentador importa estos a nivel de módulo: si faltan, la ingesta falla.
# Los modelos de spaCy (es_core_news_md, en_core_web_md, pt_core_news_md,
# de_core_news_md, fr_core_news_md) se descargan aparte con:
#   python -m spacy download es_core_news_md en_core_web_md pt_core_news_md de_core_news_md fr_core_news_md
# Los modelos Stanza de español (coref) se descargan con scripts/ensure_stanza.py.
spacy==3.8.16
stanza==1.11.1
rapidfuzz==3.14.5
hnswlib==0.8.0
sentence-transformers==5.7.0
scipy==1.16.3
scikit-learn==1.6.1
numpy==2.1.3
pyyaml==6.0.3   # config/languages.yaml (scripts/ensure_languages.py)

# Testing
pytest==9.1.1
